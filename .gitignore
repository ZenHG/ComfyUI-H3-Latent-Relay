# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.ruff_cache/

# ---- 运行时产物（本包产生的续接 latent / 备份，不入库）----
*.safetensors
*.bak*
*.ckpt
*.pt
*.pth
*.gguf

# ---- 编辑器 / OS ----
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# ---- 构建产物：最小分发集（make_minimal_bundle.py 产出）----
dist/

# ---- 日志 / 本地缓存 / 测试产物 ----
*.log
.coverage
.cache/
output/
tmp/

# ---- 密钥 / 环境变量（绝不入库）----
.env
.env.*
*.local
secrets.json

# ---- Node 工具链（若将来扩展前端）----
node_modules/

# ---- ZCode 会话本地计划（不入库）----
.zcode/

# ---- 本地调研笔记（不入库）----
RESEARCH_*.md

# ---- 本地维护规范 / 自读文档（不入库）----
# 记的是内部维护纪律与已犯过的错（含本机路径、错误数字的沿革），对使用者无价值，
# 且细节随版本变 ⇒ 放进公开仓库只会多一份会漂的文档。
LOCAL-*.md

# ---- 本机 UI 模板副本 / 本地测试夹具（各人机器不同，不入库）----
local_tests/

# ---- 离线备份（git bundle，不入库）----
*.bundle
