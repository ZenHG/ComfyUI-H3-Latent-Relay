# 贡献指南

感谢你考虑为本项目贡献代码。

## 开发环境
- Python ≥ 3.10，需要 `torch` 与 `safetensors`。
- 把本目录放进 `ComfyUI/custom_nodes/` 即可被加载；纯算法层改动也可脱离 ComfyUI 单测。

## 离线自测（提 PR 前必须跑通）
```bash
python tests/test_relay_core.py
```
- 零 GPU、不加载模型、秒级完成。会自动上溯定位 ComfyUI 根目录；装在别处时用
  `COMFYUI_PATH=/path/to/ComfyUI python tests/test_relay_core.py` 指定。
  注意：脚本本身需要**能 import 到 ComfyUI**（`comfy.nested_tensor` / `node_helpers` /
  `folder_paths`）——`relay_core` 模块可独立复用，但这份打包测试**不能**脱离 ComfyUI 跑。
- 覆盖二十四个方面（**实测执行 300 项断言**），是本包正确性的主要保障。
  报告里请贴实际执行数，不要按源码行数统计（互斥分支不会同时执行）。
- **改动后必须同步这几处计数与清单**（v0.4.2 起列为纪律，`tools/review_050.py` 会逐项核）：
  `__init__.py` 头注释节点清单 · 本文件的「方面数/断言数」· `tests/test_relay_core.py`
  头注释覆盖清单 · 版本号三处（`__init__.py` / `pyproject.toml` / `CHANGES.md` 顶部）·
  `README.md` 节点表与参数表 · `requirements.txt`。

## 代码纪律

- 🔴 **铁律一：UI 与 API 必须同一套实现，且必须基于节点**（2026-09-21 定）。

  本包的用户里**多数是用 ComfyUI 画布手动跑的**（改段号 → 点 Queue → 改 prompt → 再点 Queue），
  只有少数走 API 提交图 JSON。所以"只在脚本里生效"的改动，等于对多数用户**不存在**。

  | 允许 | 不允许 |
  |---|---|
  | 做成**节点的输入 / 输出**（widget 或连线） | 把功能藏在**外部脚本**里（`ffmpeg` 后处理、Python 换音轨、旁路 remux） |
  | 画布手点与 API 提交**走同一段 `relay_core` 代码** | 只有 API 路径上才有的分支（"脚本里有、节点里没有"，或反过来） |
  | 效果在节点 `report` / 画布报错里**可见可自证** | 只有写进日志、用户永远看不到的"隐式行为" |

  **PR 自查三条**（缺一即不算完成）：
  1. 这个功能能不能**只靠画布连线 + widget** 用上？
  2. 手动点 Queue 与 API 提交，是否**落到同一段实现**（而不是两条路各写一遍）？
  3. 效果是否在**节点 report / 画布报错**里可见（用户不读源码也能自证）？

  ⚠️ 真实反例：`--across 0.25` 这类"组装层 crossfade"只在私有脚本里改 ⇒
  开源用户一点都拿不到；而节点里**同一病因**的路径（`joined` 的旧缩短语义）默认开着却没人管。
  `tools/review_050.py` L10 会机检本条目是否还在。

- 🔴 **铁律二：音画同步不靠组装层兜**。段的音频长度 = 视频长度是**节点侧契约**
  （`裁重叠` 音视频同裁 + `音频缝` 长度守恒）；任何会**缩短时间轴**的操作
  （重叠交叉淡变 = overlap-add）都不得出现在默认档上，且不得静默生效 —— 必须 `raise`。
  对应断言：`tests/test_relay_core.py` 第 23 组。

- 🔴 **铁律三：「给建议 / 给方案」必须把**好与坏**都写透，不许只说一半**（GG 2026-09-21 定）。

  **规则**：任何「要不要做 X」的建议，必须**同时**给出下面四块 —— 缺一块就不算建议，不许给结论：

  | 必须写 | 内容 |
  |---|---|
  | **收益** | 做它能得到什么（哪条路被堵住 / 哪个数字会变好 / **谁受益：画布手动用户 vs API 提交者**） |
  | **代价** | 要改哪些文件 / 会破哪些既有断言或行为 / 连带要同步哪些计数与文档 |
  | **退路** | 出错时退到哪一步（`raise` 还是「退回旧行为 + 告警」），**两条路各自的后果**是什么 |
  | **能力边界** | 它**做不到**什么。不写边界的收益描述 = **虚假安全感** |

  **反例（本仓真实发生，2026-09-21）**：被问「要不要堵 `tile>0` 的床窗语音缺口」时，
  我先只说了半句 ——「`quietest_window` 天然避开高能量区，所以风险低于 tail 窗那次」，
  听上去像「不急」。两分钟后量出来：默认档是 `select=tail`、**根本不走 quietest**，
  瓦片 = 床源尾部 1.2s、**voiced 62.5%**、还被 loop **重复 2 遍** —— 结论完全反过来。
  **半句结论比不给结论更危险。**

  ⇒ **判定法：「只说收益」或「只报风险」的回复，一律视为未完成，必须重写。**

- 🗓 **已知缺口登记（2026-09-21）→ ✅ 已根修（同日 0.6.4，第 24 组 11 条断言锁定）**

  `build_bed` 的 `tile_samples <= 0` 分支已过 `bed_window_voiced_fraction`；
  **`tile > 0` 分支未过**，而 `tile > 0` 恰恰是 `chain_auto.sh` 的**默认档**（`TILE_W=1.2`）。
  实测（床源 = 段1，台词覆盖 3.0–4.0s）：

  | 档位 | 瓦片区间 | voiced 占比 | 后果 |
  |---|---|---|---|
  | `tile=1.2` + `select=tail`（**默认**） | [3.250, 4.450] | **62.5%** | 🔴 撞语音；`k=2` ⇒ **语音在补丁区出现 2 遍** |
  | `tile=2.0` + `select=tail` | [2.450, 4.450] | **52.5%** | 🔴 同上 |
  | `select=quiet`（`tile` 任意） | 取自最静窗 | **0%** | ✅ quietest 天然避开 |

  **根修（0.6.4，按 GG「从根本上处理掉」执行）**：所有取床源路径统一走
  `pick_bed_window`（返回 ``起点 / 是否换窗 / 首选窗占比 / 最终窗占比``）；瓦片档阈值
  `AUDIO_SEAM_BED_TILE_VOICED_FRAC = 0`（**任何**有声帧都判撞 —— 瓦片会被环铺 k 遍）；
  E5（`exp_bed_jitter`）在瓦片档**不再被静默忽略**（错开映射成合法候选序号）；
  全源皆撞 ⇒ tail 档退回**最静窗**（语音残留最少）+ report ⚠，**不 raise**；
  报告显式给出「判据阈值 / 原占比 / 已换窗位」。候选遍历先算一次帧 RMS（O(T)，原 O(T²)）。
  `nodes.py` 的 E5 tooltip 同步删掉「仅在 tile_seconds=0 时生效」的旧限制。
  测试：`tests/test_relay_core.py` 第 24 组 **14 条**（300/0）；`22.8` 环铺断言不受影响
  （新窗合成素材上 `_d_step`/`_t_step` 分布未变）。
  **P0 持续帧滤波**（同日追加）：`AUDIO_SEAM_BED_VOICED_SUSTAIN = 2` —— 连续 ≥2 帧
  才计有声帧；瓦片档（0% 阈值）从此不被单帧瞬态触发；连续台词实测无损。

  **能力边界**：该判据是**能量型**（帧 RMS 超**全源中位 3×**记为有声帧）⇒ 只能抓住
  「**安静底噪里冒出来的语音**」（雨声 + 台词正是这种）；**素材本身以语音为底噪时不报**
  （BGM / 连续旁白抬高全源中位 ⇒ 语音帧不达 3×）。⇒ **过了这条检查 ≠「对白重叠」绝迹。**

  **同族约定要一致**：`bed_window_voiced_fraction` 接受 3D `[B,C,T]`，`quietest_window`
  只接受 2D `[C,T]` ⇒ 以 3D 调 `build_bed` 时**检查能过、退路会崩**
  （`RuntimeError: Tensors must have same number of dimensions`，报错零指向性）。
  产线走 `_audio_parts`（2D）故为潜伏缺陷。

  **只影响「一个功能点」—— 谁能撞、谁撞不到（按实际用法列全）**

  受影响的功能**只有一个**：`H3RelayAudioSeam` 的「头部补丁」在 `patch_seconds>0` 时**用什么素材**。
  `patch_seconds=0` 时整个补丁分支不进（`audio_seam_patch` 早退）⇒ 与之无关的功能一律不受影响：

  | 用法 | `patch_seconds` | `tile_seconds` | 会撞吗 |
  |---|---|---|---|
  | **UI 默认**（advanced 不展开、不改） | `0.0` | `0.0` | ❌ 不进补丁分支 |
  | UI 按 tooltip 填"建议 2.0"、`tile` 留 0 | `2.0` | `0.0` | ❌ **已修**（`tile<=0` 分支已过检查） |
  | UI 再按 tooltip 填 `tile=1.2` | `2.0` | `1.2` | 🔴 **撞**（本缺口） |
  | `chain_auto.sh` 照抄默认值跑 | `2.0` | `1.2` | 🔴 **撞**（本缺口） |
  | 我们自己的实跑（显式覆盖过） | `0` 或 `1.2` | `0` | ❌ |

  **不受影响的功能**（明确列出，免得误判）：第 3 路输出 `joined`（走 `join_audio_segments`，与床源无关）、
  音画同步/长度守恒（本分支长度守恒）、落盘格式与耗时、`TrimAV` 裁切、后处理 `Post`、桥的两条路径。
  ⚠️ **`exp_bed_jitter`（E5）在 `tile>0` 时本来就被静默忽略** —— 那是**另一个**已记录的缺口，堵本缺口不会修它。

  **堵 / 不堵 各自的后果**

  | | 不堵（现状） | 堵（`tile` 分支也过检查） |
  |---|---|---|
  | **收益** | 无（保持现状） | `patch>0 + tile>0` 的人不再听到**上一段台词片段在补丁区重复 2 遍**；把"参数一换保护就消失"的隐性不一致消掉，可写成断言 |
  | **代价** | 照抄 `chain_auto.sh` 默认值的人**必现**台词重复 | ① 瓦片起点会变 ⇒ `22.8 瓦片自叠化环铺`（`_d_step`/`_t_step`）要重跑/调整，断言数 → 287 ⇒ README/CONTRIBUTING/docs-08 计数连带同步；② 退路两难（见下）；③ 可能改变 `tile>0` 用户的成片（哪怕只是瓦片起点） |
  | **退路** | —— | **`raise` 不行**：素材本身以语音为底噪（BGM/连续旁白）时会把合法素材判死，而 `TILE_W` 默认非 0 ⇒ **默认档直接报错 = 硬回归**。**只能「退回旧行为 + report 告警」** ⇒ 不报错，但问题依旧（收益缩水为"可见性"） |
  | **对我们产线** | 0（实跑都 `TILE=0`） | 0 |

  **更省事的替代路径（成本远低于改 `relay_core`）**：把 `chain_auto.sh` 的 `TILE_W` 默认
  **`1.2 → 0`** —— 零代码风险、零断言连锁，**默认档立刻走已修的 `tile<=0` 路径**；
  想用床环铺的人显式设 1.2。代价：`tile` 的设计用途（"干净窗短于补丁时"）从"默认开"变成"要手动开"。

- **硬错误必须 raise，绝不静默降级**（如帧数不在网格上、分辨率不匹配、段号与取源矛盾）。
- `relay_core.py` 是纯算法层，**不得 import 任何 ComfyUI 模块**，以保证可独立单测与复用。
- 新增 / 修改节点时，请同步更新 `README.md`（节点表 / 参数表 / 排障表）与 `CHANGES.md`。

## 许可（提 PR 前必读）

- 提交 PR 即表示：你对你提交的代码拥有处分权，并同意以 **MIT** 许可随本仓库一同发布。
- 🔴 **不得引入 GPL / AGPL / LGPL 或其它 copyleft 项目的代码、注释结构或逐字表达。**
  本仓库主张 MIT，而"机制不受版权保护、表达受保护"——照抄一个函数体哪怕改了函数名也算。
  本仓库曾因此**整体重写过一次**，见 [`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md) §一·B。
- 参考第三方实现时：只取**机制**，自己写**表达**；并在 NOTICES 的 §一 表里补一行出处。
- ⚠️ 运行时宿主 **ComfyUI 是 GPL-3.0**（我们 import 它、但不复制它），见 NOTICES §一·C。
  新增 `import comfy.*` 之前先想清楚这一步的耦合代价。

## 提交信息
- 建议清晰说明「改了什么 / 为什么」，并关联相关 Issue。中英文均可。
- 提交请使用**非个人敏感**的 git 身份（例如 GitHub 提供的 no-reply 邮箱），避免把私人邮箱写进公开历史。
